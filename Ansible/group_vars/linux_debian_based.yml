---
# Debian/Ubuntu group variables
ssh_pub_key: "{{ lookup('file', './ssh_keys/ansible.pub', errors='ignore') | default('') }}"

ufw_allowed_ports:
  - { port: 22, proto: "tcp" }
#  - { port: 80, proto: "tcp" }
#  - { port: 443, proto: "tcp" }

install_docker: false
